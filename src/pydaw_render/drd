PYDAW_VERSION=$(cat ../major-version.txt)
BIN=$PYDAW_VERSION"_render-dbg"
PROJECT=$HOME/$PYDAW_VERSION/default-project

make clean && \
make debug || \
exit $?

valgrind --tool=drd \
    ./$BIN $PROJECT "test.wav" 0 0 3 3 44100 512 2 0 --no-file

